---
name: "speckit.analyze"
description: "Run read-only consistency and quality analysis across spec, plan, and tasks artifacts."
agent: "Orchestrator"
argument-hint: "Optional analysis context"
---

Follow the exact workflow and constraints defined in
[`../../.claude/commands/speckit.analyze.md`](../../.claude/commands/speckit.analyze.md).

Apply it to this input:

`$ARGUMENTS`
