# Tasks: Limpieza incremental picks_service

- [ ] 1. Ejecutar flake8 sólo para `picks_service.py` y guardar el reporte (done).
- [ ] 2. Eliminar imports no usados (commit: `refactor(picks): remove unused imports`).
- [ ] 3. Eliminar trailing whitespace y W29x (commit: `style(picks): trim trailing whitespace`).
- [ ] 4. Corregir F541 (f-strings vacíos) si aparecen (commit: `fix(picks): correct f-string`).
- [ ] 5. Ejecutar `flake8` + `pytest` y verificar.
